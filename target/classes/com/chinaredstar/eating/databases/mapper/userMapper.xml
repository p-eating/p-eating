<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinaredstar.eating.mapper.DemoMapper">

    <select id="findUser" resultType="User">
        select employee_id as 'openId', name, market_code as 'marketCode', market_name as 'marketName'
        FROM t_sync_esb_employee where employee_id = '51032488'
    </select>

</mapper>